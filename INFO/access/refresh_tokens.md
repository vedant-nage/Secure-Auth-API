# JWT==Access Tokens

# Refresh and Access token : 
Access tokens are short lived , we can set the expiry of the token And these tokens are use the access the protected APIs

Refresh tokens are long lived tokens which are use to get the new access tokens 

Access tokens are sent with protected API requests where the server needs to identify the user, such as fetching profile data, user-specific history, or performing authenticated actions.